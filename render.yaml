services:
  - type: web
    name: memogram-frontend
    runtime: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://memogram-backend.onrender.com
    routes:
      - type: rewrite
        source: /**
        destination: /index.html
